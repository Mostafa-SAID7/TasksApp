5. Services

AuthService: login/register/logout using inject(HttpClient)

ProjectService: CRUD + team invites

TaskService: CRUD + drag/drop reorder

ThemeService: signal<boolean> for dark mode persisted to localStorage