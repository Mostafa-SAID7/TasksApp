/**
 * The API is served from the same origin as the Angular application in
 * development and production. Keeping this relative avoids binding the
 * client to a machine-local host or a deployment-specific domain.
 */
export const API_URL = '/api';